[user]
    name = Matteus
    email = post@kalis.no
[color]
    ui = true
[alias]
    st = status
    l0 = log -n25 --graph --decorate --oneline --abbrev-commit --all
    l = log -n25 --graph --abbrev-commit --decorate --format=format:'%C(yellow)%h%C(reset)%C(auto)%d%C(reset) %C(dim white)%an%C(reset) %C(normal)%s%C(reset) %C(bold blue)(%ar)%C(reset)' --all
    up = pull --rebase --autostash
[credential "https://github.com"]
    username = matkalis
[core]
	editor = vim
	compression = 0
  pager = less -+$LESS -FRX
[push]
    default = simple
[http]
	postBuffer = 157286400
	sslVerify = false
[init]
	defaultBranch = main
